using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Navigation;
using Microsoft.Phone.Controls;
using Microsoft.Phone.Shell;
using myanumber.Resources;
using myanumber.ViewModels;
using myanumber.Resources;

namespace myanumber
{
    public partial class MainPage : PhoneApplicationPage
    {
        // Constructor
        //bool hasLinkedNumber = false;
        public MainPage()
        {
            InitializeComponent();

            LinkedNumberTextBox.Text = AppResources.SecondaryWaterMarkText;
        }

        //private void NextButton_Click(object sender, RoutedEventArgs e)
        //{

        //     NavigationService.Navigate(new Uri("/EnterPin.xaml", UriKind.Relative));
        //}

        private void LinkedNumberTextBox_KeyDown(object sender, System.Windows.Input.KeyEventArgs e)
        {
            try
            {
                if (LinkedNumberTextBox.Text.ToString().StartsWith("0") || LinkedNumberTextBox.Text.ToString().StartsWith("1"))
                {
                    LinkedNumberTextBox.MaxLength = 11;
                    //isSecondNumberValid = true;
                    //NextButton.Style = (Style)Application.Current.Resources["EnableButtonStyle"];
                }
                else
                {
                    LinkedNumberTextBox.MaxLength = 10;
                    //isSecondNumberValid = true;
                    // NextButton.Style = (Style)Application.Current.Resources["EnableButtonStyle"];
                }
                NextButton.Style = (Style)Application.Current.Resources["EnableButtonStyle"];
            }
            catch (Exception ex) 
            {
                throw;
            }
        }

        private void LinkedNumberTextBox_GotFocus(object sender, RoutedEventArgs e)
        {
            try
            {
                LinkedNumberTextBox.Text = "";
                //hasLinkedNumber = true;
                NavigationService.Navigate(new Uri("/EnterLinkedNumber.xaml", UriKind.Relative));
            }
            catch (Exception ex) 
            {
                throw;
            }
        }


        private void LinkedNumberTextBox_LostFocus(object sender, RoutedEventArgs e)
        {
            try
            {
                //if (!hasLinkedNumber)
                //{
                //    LinkedNumberTextBox.Text = AppResources.SecondaryWaterMarkText;
                //}

                //hasLinkedNumber = false;

            }
            catch (Exception ex) 
            {
                throw;
            }
        }
    }
}