using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Navigation;
using Microsoft.Phone.Controls;
using Microsoft.Phone.Shell;
namespace myanumber
{
    public partial class EnterPin : PhoneApplicationPage
    {
        public EnterPin()
        {
            InitializeComponent();
        }

        protected override void OnNavigatedTo(System.Windows.Navigation.NavigationEventArgs e)
        {
            base.OnNavigatedTo(e);
            if (PhoneApplicationService.Current.State.ContainsKey("LinkedNumber"))
            {
                SecondaryPhoneNumber.Text = (string)PhoneApplicationService.Current.State["LinkedNumber"];
            }
        }

        private void PinCodeTextBox_GotFocus(object sender, RoutedEventArgs e)
        {
            NavigationService.Navigate(new Uri("/EnterCode.xaml", UriKind.Relative));
        }
    }
}